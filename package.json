{
  "name": "gelatin",
  "version": "0.1.0",
  "description": "libPlasma bindings/implementation",
  "main": "lib/index.js",
  "scripts": {
    "doc": "jsdoc -c doc/conf.json -d doc/out lib/*.js",
    "justthetest": "tap test/*.js",
    "test": "node-gyp build && npm run justthetest",
    "test-debug": "node-gyp build --debug && GELATIN_DEBUG=1 npm run justthetest"
  },
  "author": "Tom Jakubowski",
  "license": "MIT",
  "dependencies": {
    "nan": "^2.3.1"
  },
  "gypfile": true,
  "private": true,
  "devDependencies": {
    "jsdoc": "^3.4.0",
    "tap": "^5.4.2"
  }
}
